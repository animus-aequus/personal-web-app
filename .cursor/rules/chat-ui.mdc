---
description: Chat UI, useChat transport, Zustand message store, and merge logic
globs: src/components/chat/**,src/lib/stores/**,src/components/layout/site-shell.tsx,src/lib/access/**,src/components/access/**
alwaysApply: false
---

# Chat UI and client state

Read `docs/architecture.md` (Message merge, Session identity) before non-trivial changes.

## Components

| File | Role |
|------|------|
| `site-shell.tsx` | Turnstile + i18n + `AppShell` chrome; no `useChatSession` |
| `app-human-gate.tsx` | App-level Turnstile before any `(site)` view |
| `route-access-gate.tsx` | `/chat` layout host for the access engine |
| `chat-page-client.tsx` | `useChatSession` + chat surface after the pause gate |
| `chat-panel.tsx` | `TextChatArea`, voice toggle, `useChat` / `useSession`, merge (`ReadyChatSurface`) |
| `message-list.tsx` | Renders `ChatMessage[]` |
| `message-input.tsx` | Text send callback |

## Session bootstrap

- App Turnstile (`AppHumanGate`) runs in `SiteShell` for the `(site)` layout lifetime
- `/chat` layout `RouteAccessGate` evaluates catalog conditions (Phase 1: `pause` only)
- `useChatSession` runs only on `/chat` after the gate; leaving `/chat` unmounts chat (no keep-alive)
- `TextChatArea` mounts only when `sessionId` exists, with `key={sessionId}`
- **Why:** `useChat` + `DefaultChatTransport` bind `sessionId` at creation time

## Text chat (`useChat`)

```tsx
new DefaultChatTransport({
  api: "/api/chat",
  prepareSendMessagesRequest: ({ messages }) => ({
    body: { sessionId, message: lastUserTextFromUiMessages(messages) },
  }),
})
```

- BFF expects `{ sessionId, message }` only — agent checkpointer holds conversation history
- Limits: **1000** chars per turn (`CHAT_MESSAGE_MAX`); paste soft-cap **1500** code points in control bar (`CHAT_MESSAGE_INPUT_CEILING`)
- Map AI SDK `UIMessage` → `ChatMessage` with `source: "text"`
- Loading state from `status === "submitted" | "streaming"`

## Zustand store (`chat-store.ts`)

- Persist key: `personal-agent-chat`
- Fields: `sessionId`, `messages`, `voiceMessageIds` (dedup set for voice rows)
- `addVoiceMessage` — idempotent by message id
- Voice messages always have `source: "voice"`

## Merge in `chat-panel.tsx`

```tsx
[...voiceMessages, ...textMessages].sort((a, b) => a.timestamp - b.timestamp)
```

- `voiceMessages`: filtered from store (`source === "voice"`)
- Do not write voice turns from `useSessionMessages` into the store

## Voice toggle handler

- Enable: set new `voiceConnectionId` + `voiceEnabled true` in same toggle
- Disable: `voiceEnabled false` → effect cleanup calls `session.end()`
- On `start()` failure: `onVoiceDisconnect()` resets toggle

## Do not

- Mount/unmount `useSession` when toggling voice (see rule `livekit-voice`)
- Use session transcriptions as persisted chat history
- Create `useChat` before `sessionId` is known
- Keep chat mounted in the `(site)` layout across `/chat` ↔ `/terms` / `/about-me`
