---
description: Security controls — read docs/security.md for principles and implemented controls
alwaysApply: true
---

# Security documentation

When working on **security**, **rate limits**, **Turnstile**, **session cookies**, **booking proxy routes**, or **Route Handler hardening**:

1. Read [`docs/security.md`](../../docs/security.md) for principles and **Implemented controls**.
2. After changing a control: update **Implemented controls** in `docs/security.md`.

Cross-repo agent API context (if `personal-voice-agent` is open): `../personal-voice-agent/docs/security.md`.

Do not duplicate long security prose in code comments — keep durable facts in `docs/security.md` only.

If `docs/tmp/.active` is `security` (tmp enabled by user), also read `docs/tmp/security/STATUS.md` per rule `docs-tmp`.
